---
alwaysApply: false
---

#### 생성 예시

```tsx
import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { motion } from "framer-motion";

interface ProductCardProps {
  title: string;
  price: number;
}

export default function ProductCard({ title, price }: ProductCardProps) {
  return (
    <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }}>
      <Card className="w-full rounded-2xl shadow-lg p-4">
        <CardContent>
          <h2 className="text-xl font-semibold">{title}</h2>
          <p className="mt-2 text-lg">{price.toLocaleString()}원</p>
          <Button className="mt-4 w-full">구매하기</Button>
        </CardContent>
      </Card>
    </motion.div>
  );
}
```

* Tailwind **`rounded-2xl` + `shadow-lg`** 기본 적용
* 이미지가 있을 땐 `next/image` 사용
* 모든 버튼·폼 요소는 shadcn/ui 컴포넌트로 교체
