'use client';

import { ArticleDetail } from '@/widgets/article-detail/ui/ArticleDetail';

/**
 * [Article Detail Page]
 * 글 상세 페이지입니다.
 * Widget(ArticleDetail)을 배치하는 얇은 껍데기 역할만 합니다.
 */
export default function ArticleDetailPage() {
  return <ArticleDetail />;
}