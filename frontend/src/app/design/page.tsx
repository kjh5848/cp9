import React from 'react';
import { DesignTheme } from '@/widgets/design-theme/ui/DesignTheme';

/**
 * [Design Page]
 * 아티클 디자인 테마를 설정하는 페이지입니다.
 * Widget(DesignTheme)을 배치하는 얇은 껍데기 역할만 합니다.
 */
export default function DesignPage() {
  return <DesignTheme />;
}
